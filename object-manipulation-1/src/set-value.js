/* exported setValue */

// define function setValue
// use provided object
// set new property with new value

function setValue(object, key, value) {
  object[key] = value;
}

// bracket notation to create a property named key that is assigned the provided value
