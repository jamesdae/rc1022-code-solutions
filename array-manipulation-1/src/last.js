/* exported last */

// returns the value of the element at the last index of given array

function last(array) {
  return array[array.length - 1];
}

// uses the length property of the array object subtracted by 1 to calculate the last index
// returns the value of array at the last index
