/* exported getLastChar */

// define function getLastChar
// return the value of a given string's last character

function getLastChar(string) {
  return string[string.length - 1];
}

// function receives string as parameter
// accesses string at last index represented by length of string minus 1
// returns value at end of function
